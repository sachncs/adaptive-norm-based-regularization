# Adaptive Norm-Based Regularization - Environment Configuration
#
# This project does not require environment variables for standard usage.
# All hyperparameters are passed programmatically or via CLI arguments.
#
# The following variables are optional and only needed for advanced usage:

# No environment variables required for standard usage.
# See demo/run_simulation.py and demo/run_real_data.py for CLI arguments.
